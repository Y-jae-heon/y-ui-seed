export enum Side {
  TOP = "top",
  RIGHT = "right",
  BOTTOM = "bottom",
  LEFT = "left",
}

export enum Orientation {
  VERTICAL = "vertical",
  HORIZONTAL = "horizontal",
}

export enum RoundedTypes {
  DEFAULT = "default",
  MEDIUM = "medium",
  LARGE = "large",
  FULL = "full",
}
