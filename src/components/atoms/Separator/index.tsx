"use client";
import { Separator as SeparatorUI } from "@components/ui";
import { SeparatorProps } from "./types";

export default function Separator(props: SeparatorProps) {
  return <SeparatorUI {...props} />;
}
