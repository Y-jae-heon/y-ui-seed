import { RoundedTypes } from "../common-types";

export interface SkeletonProps extends React.HTMLAttributes<HTMLDivElement> {
  // height?: string;
  // width?: string;
  // rounded?: RoundedTypes;
  className?: string;
}
